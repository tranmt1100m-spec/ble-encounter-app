# REPORT — 自前サーバー移行の実機検証と開門表示バグ修正（beta1.7.10+43）

作業日: 2026-08-16 / 検証: 実機2台（Pixel 5・A202SH）＋ 追加修正

---

## ☕ 3分要約

1. **Supabaseから自前サーバー（さくらVPS）への移行を実機で確認完了** —
   匿名登録・トークン発行・すれ違い解決・プロフィール同期・開門まで一気通貫で成功
2. **移行検証中に重大バグを発見して修正** — 起動のたびに匿名ユーザーが重複作成され、
   実機2台なのにサーバー上に**7ユーザー**が出来ていた。修正後は**2ユーザー**で一致
3. 双方向のすれ違い解決に成功（Pixel→SharpB / A202SH→PixelA、いずれも1件解決）
4. 21:00の夜の開門で相手プロフィール（名前・アバター色・ひとこと）が正しく表示
5. サーバー実測メモリ: api 5.6MB / caddy 13.6MB（共有サーバーへの負荷は無視できる範囲）

---

## 🚧 ゲームタブを「近日公開」表示に一時変更（beta1.7.11+44）

**背景**: リリースを急ぐにあたり、4種のミニゲーム（ピース集め・タワーRPG・
水族館・しんけいすいじゃく）は未検証の不具合が多く残っている。
今回の21時バグ修正で「今日出会った人」の供給経路は直ったが、ゲーム側の
実装そのものは個別に検証できていないため、リリース対象からは一旦外す。

**対処**: `kGameTabEnabled`（[ble_config.dart](lib/core/ble_config.dart)）を
`false` にし、ゲームタブを [ComingSoonScreen](lib/ui/coming_soon_screen.dart)
に差し替え。**ゲーム自体のコードは削除していない** — フラグを `true` に
戻すだけで元の実装に復帰する。

**やっていないこと**: 4ゲームそれぞれの不具合の切り分け・修正。
公開後に手が空いたタイミングで個別に検証する。

---

## 🐛 21時以降のすれ違いが画面から消えるバグ

**症状**: 「出会っているのに何も起こらない」。21時〜24時にすれ違うと、
きょうの広場のどこにも現れず、開門行にも人数が出ない。

**原因**: `gateTimeFor` は21時以降のすれ違いを**翌朝9時の開門**へ割り当てるが、
画面側は「今日の9時・12時・21時」の3つしか並べていなかった。
そのため21〜24時のすれ違いはどの行にも該当せず、翌朝まで完全に不可視だった。
ゲート割り当て自体は正しく、**表示する開門の一覧が足りていなかった**のが原因。

**修正**:

| 対処 | 内容 |
|---|---|
| 表示一覧の是正 | 21時を過ぎたら「あしたの朝の開門」も並べる（[ble_providers.dart](lib/providers/ble_providers.dart) `gatesToShow`） |
| 判定の集約 | 画面側の私有ロジックを純粋関数へ切り出し、単体テスト可能にした |
| ラベル | 日付が翌日の開門は「あしたの朝の開門」と表示して区別 |

**回帰テスト**（4件追加・計41件）:
不変条件「**どの時刻のすれ違いも必ずいずれかの開門行に現れる**」を
0〜23時×3分点で検証。0時バグ・18時バグと同じ枠組みで固定化した。

**未検証**: 実機が接続されていないため、画面での確認は未実施。

---

## 🐛 匿名登録の重複バグ

**症状**: 実機2台で起動しただけで、サーバーに7ユーザーが作られていた。
発行したトークンと同期先の人格がずれるため、すれ違いが解決できない。
Pixel側では `Null check operator used on a null value` も発生していた。

**原因**: `ApiService.init()` に多重実行防止が無く、SecureStorageへの保存が終わる前の
`read()` が null を返すたびに匿名登録が走っていた。さらに読み出し結果を無条件に
フィールドへ代入していたため、並行する登録処理の結果を null で上書きしていた。

**修正** ([lib/services/api_service.dart](lib/services/api_service.dart)):

| 対処 | 内容 |
|---|---|
| single-flight化 | `init()` / `_signUpAnonymously()` は進行中のFutureを共有し実処理は1回だけ |
| null上書きの排除 | 資格情報は取得できた場合のみフィールドへ反映 |
| 登録直前の再確認 | 別経路が先に登録を終えていればそれを再利用し、新規作成しない |
| 保存順の是正 | SecureStorageへ保存してからフィールドへ反映（並行読み出しとの整合） |

---

## ✅ 実機検証の結果

| 確認項目 | 結果 |
|---|---|
| 匿名登録 | 各端末1回のみ。DB上 users=2（修正前は7） |
| 資格情報の永続化 | 再起動時は `restored uid=...`（再登録なし） |
| トークン発行 | 各端末1本・24時間有効 |
| BLEすれ違い検知 | 双方向で相手トークンを受信 |
| トークン解決 | Pixel: 1 new / A202SH: 1 new（双方向成功） |
| プロフィール同期 | 名前・アバター色・ひとことが相手側に正しく表示 |
| 夜の開門(21:00) | 「1人 あける！」→ 開封カード表示 → かんりょう まで正常 |
| サーバー負荷 | api 5.6MB / caddy 13.6MB |

`flutter analyze` エラー0 / `flutter test` 37件全パス。

---

## 📝 気づき（未対応）

- **横向き時のオンボーディングが窮屈** — A202SHを横向きで起動すると、
  説明文とページインジケータが重なる。縦向きでは問題なし。
  実用上は縦利用が前提だが、回転対応するなら要調整。

---

## 参照

- サーバー構成・運用手順: [server/README.md](server/README.md)
- 恒久ルール: [CLAUDE.md](CLAUDE.md)
